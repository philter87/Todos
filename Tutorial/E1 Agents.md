# Custom agents (Subagents)

How to
- Tandhjul --> Custom Agents --> Generate agent...
- /create-agent

Prompt 1

`Create an agent for doing security reviews...` 

`Create an agent to do Quality Assurance on a new feature. It should use Playwright to test if everything works when the implementation is done. Both the frontend and the backend needs to be running before running explorative tests with Playwright`

Notes
?