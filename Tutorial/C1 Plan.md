# Plan mode 

Prompt: 

`Add support for deleting a Todo item. Add an endpoint in the backend and add the required change in the frontend. Remember to add an integration test` 


```bash
git checkout feat/plan-1
```